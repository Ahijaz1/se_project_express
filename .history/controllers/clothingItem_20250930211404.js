const clothingItems = require("../models/clothingItem");

const createItem = (req, res) => {
  console.log(req);
  console.log(req.body);

  const { name, weather, imageUrl } = req.body;

  ClothingItems.create({ name: name, weather: weather, imageUrl: imageUrl });
};
